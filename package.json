{
  "name": "axit",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:css": "mkdir -p public/css && stylus stylus/index.styl -o public/css/ --print --sourcemap > public/css/style.css",
    "build:js": "mkdir -p public/js && esbuild js/entry.js --bundle --outfile=public/js/all.js --target=es2015 --sourcemap",
    "build": "npm run build:css && npm run build:js",
    "watch:css": "onchange 'stylus/**/*.styl' -- npm run build:css",
    "watch:js": "onchange 'js/**/*.js' -- npm run build:js",
    "watch": "npm run watch:css & npm run watch:js",
    "dev": "npm run build && npm run watch & browser-sync start --server public --files public"
  },
  "dependencies": {
    "browser-sync": "^3.0.4",
    "esbuild": "^0.27.4",
    "onchange": "^7.1.0",
    "stylus": "^0.64.0"
  }
}
